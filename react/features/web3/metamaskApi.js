/**
 * A singleton for loading and interacting with the metamask API.
 */
const metamaskApi = {
    /**
     * Obtains Metamask API Client Library, loading the library dynamically if
     * needed.
     *
     * @returns {Promise}
     */
    get() {
        return {}//Promise.resolve(globalGoogleApi);
    },

    
};

export default metamaskApi;
